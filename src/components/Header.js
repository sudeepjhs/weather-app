import { Navbar } from "@nextui-org/react";

const Header = () => {
  return (
    <Navbar className="bg-green-500 text-white">
      <div className="justify-center w-full flex">Weather APP</div>
    </Navbar>
  );
};

export default Header;
